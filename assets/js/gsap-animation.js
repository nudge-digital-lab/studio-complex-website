(function ($) {
	"use strict";

	/* Animaciones deshabilitadas a pedido del cliente. El contenido se
	   muestra directo, sin fade-in, split-text, parallax ni scroll suave. */
	window.gsapController = function () {};
})(jQuery);
